# Redis

Production-oriented Redis learning focused initially on the **caching use case**.

The initial scope includes cache architecture, key/value modeling, TTL and expiration, eviction policies, cache-aside patterns, invalidation, connection management, memory management, performance, high availability considerations, observability, troubleshooting, and hands-on caching labs.

Redis topics outside the caching scope can be added later as the repository evolves.
